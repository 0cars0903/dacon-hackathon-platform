// Legacy localStorage auth tests — replaced by Supabase Auth.
// These tests are intentionally disabled.
import { describe, it, expect } from "vitest";

describe("Legacy auth (disabled)", () => {
  it("placeholder — old localStorage auth tests removed", () => {
    expect(true).toBe(true);
  });
});
