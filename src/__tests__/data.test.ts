// Legacy localStorage data tests — replaced by Supabase backend.
// These tests are intentionally disabled.
import { describe, it, expect } from "vitest";

describe("Legacy data.ts (disabled)", () => {
  it("placeholder — old localStorage tests removed", () => {
    expect(true).toBe(true);
  });
});
